// string indexing

String sample = "Hello world"; 
for (i=0;i < sample.length();i++) {
    println(sample[i]);
}

// string repetition

String sample = "Hello world"; 
for (i=0;i < sample.length();i++) {
    println(sample[i]*3);   
}

// concat

String a = "Hello "
b = a.concat("World ")
println(b*3)

// indexOf

String a = "Hello "
println(a.indexOf('lo')

// toUpper,toLower

String a = "Hello "
println(a.toUpperCase())
println(a.toLowerCase())

// string methods : https://www.tutorialspoint.com/groovy/groovy_strings.htm